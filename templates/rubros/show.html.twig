{% extends 'base.html.twig' %}

{% block title %}Rubros{% endblock %}

{% block body %}
    <h1>Rubros</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rubro.id }}</td>
            </tr>
            <tr>
                <th>Rubro</th>
                <td>{{ rubro.Rubro }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rubros_index') }}">back to list</a>

    <a href="{{ path('rubros_edit', {'id': rubro.id}) }}">edit</a>

    {{ include('rubros/_delete_form.html.twig') }}
{% endblock %}
